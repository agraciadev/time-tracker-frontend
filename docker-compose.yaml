version: "3.9"
services:
  node:
    build:
      context: ./docker/node
    volumes:
      - ./:/usr/app
    # entrypoint: ./docker/node/entrypoint.sh
    stdin_open: true 
    tty: true
    ports:
      - ${SERVER_PORT:-3000}:3000
      - 24678:24678